---
name: enfirst-bridge
version: 1.0.0
author: 老二
description: "非英语输入直译为英语理解桥：将用户的非英语输出在进入模型主推理前先直线翻译成英语，减少语言理解 token 消耗，统一跨场景（对话/编程/图片/文案/Agent）的语义理解路径。"
tags:
  - language
  - optimization
  - translation
  - token-reduction
  - agent
category: optimization
license: MIT
---

# Enfirst Bridge — 非英语输入英语理解桥

> 一句话：**用户说什么语言都行，模型先用英语理解一遍。**

## 解决的核心问题

当今主流大模型在处理非英语输入时存在一个隐性开销：

- 模型的语义空间以英语为锚定坐标系（训练语料、对齐偏好、指令微调均以英语为主）。
- 当输入是非英语（中文、日文、俄文、阿拉伯文…）时，模型需要先在内部做一次「非英语 → 英语语义」的对齐，再进入主推理。
- 这个内部对齐**消耗额外的语言理解 tokens**，且对齐路径是**隐式、可漂移**的——同样的中文句子，不同上下文下可能被对齐到略有不同的英语语义，导致理解不稳定、token 浪费、长上下文里语义偏移累积。

本 skill 的做法：**在输入进入主推理之前，先做一次显式、直线型的「非英语 → 英语」翻译**，把翻译结果作为模型的唯一理解锚点。这样：

1. **减少 token 消耗** —— 跳过模型内部隐式对齐的来回。
2. **理解路径呈直线** —— 同一输入永远翻译成同一英语锚点，语义不漂移。
3. **统一跨场景** —— 对话、编程、图片生成、文案、Agent 自动化，全部走同一条「英语理解 → 场景输出」管线。

## 核心原则（不可违反）

1. **只在非英语输入时触发**。用户输入已是英语则直接进入主推理，不做多余翻译。
2. **翻译是直线型的**：一次直译，不做意译、不做润色、不做多版本对比。目标是「语义锚点」不是「优雅译文」。
3. **翻译结果不展示给用户**（除非用户要求）。它是内部理解步骤，对用户透明。
4. **输出语言遵循 `<response_language>`** —— 用户用中文就回中文，用日语就回日语。英语理解 ≠ 英语输出。
5. **场景适配**：翻译时保留场景关键信息（代码标识符、图片生成关键词、Agent 指令动词不译）。

## 触发条件

当满足以下**任一**条件时触发 Enfirst Bridge：

- 用户输入包含非英语文字（CJK 统一表意文字、西里尔字母、阿拉伯字母、天城文等）。
- 用户输入的 `<response_language>` 不是英语。
- 用户明确要求"用英语理解"或"先翻译再处理"。

**不触发**：纯英语输入、纯代码输入、纯数学公式输入。

## 工作流程（5 步直线型管线）

```
用户输入（非英语）
        │
        ▼
[1] 语种检测 —— 判定主语种，若为英语则跳过本 skill
        │
        ▼
[2] 直线翻译 —— 非英语 → 英语，一次直译，保留场景标记
        │
        ▼
[3] 英语锚点 —— 翻译结果作为模型唯一理解输入
        │
        ▼
[4] 主推理 —— 基于英语锚点执行用户实际意图
        │
        ▼
[5] 场景输出 —— 按 <response_language> 用用户语言输出结果
```

### 关键：第 2 步的「直线翻译」规则

翻译时遵循以下子规则，保证「直线」而非「曲线」：

| 规则 | 说明 | 例子 |
|---|---|---|
| R1 直译优先 | 逐句直译，不意译、不润色 | "这个按钮太丑了" → "this button is too ugly" |
| R2 保留标识符 | 代码中的变量名、函数名、API 名、文件路径不译 | "改一下 fetchUserData 函数" → "modify the fetchUserData function" |
| R3 保留指令动词 | Agent 场景的动词原样保留为英语 | "帮我搜索一下" → "help me search" |
| R4 保留图片关键词 | 图片生成场景的关键词保留为英语供模型直接用 | "赛博朋克风格的城市" → "cyberpunk style city" |
| R5 量化词保留数字 | "三个"、"百分之二十" 译为 "three"、"twenty percent" | — |
| R6 不加解释 | 翻译结果只有译文，不加"翻译如下:"等前缀 | — |

## 场景适配（5 类场景）

本 skill 覆盖 5 类场景，每类的翻译侧重略有不同：

### 场景 A：普通对话
- **翻译侧重**：语义忠实，保留语气（疑问/命令/感叹）。
- **示例**：用户"你觉得这个方案怎么样？" → 锚点"what do you think of this plan?" → 主推理 → 中文输出。

### 场景 B：代码编程
- **翻译侧重**：保留所有标识符、路径、错误信息原文；只翻译自然语言部分。
- **示例**：用户"在 handleSubmit 里加一个表单校验，邮箱不能为空" → 锚点"add form validation in handleSubmit, email cannot be empty"。
- **注意**：代码块内的注释翻译遵循 R2（标识符不译），但注释文本可译以便理解。

### 场景 C：图片生成
- **翻译侧重**：翻译成英语关键词短语（而非完整句子），因为图片模型吃关键词。
- **示例**：用户"画一个穿汉服的女孩在樱花树下读书，水彩风格" → 锚点"a girl in hanfu reading under cherry blossom tree, watercolor style"。
- **附加**：调用 ImageGen 时直接用这个英语锚点作为 prompt。

### 场景 D：文案生成
- **翻译侧重**：翻译意图与受众，但保留品牌名、产品名、口号原文。
- **示例**：用户"给我们的产品'云小宝'写一句slogan，面向宝妈群体" → 锚点"write a slogan for our product '云小宝', targeting young mothers" → 主推理 → 中文输出 slogan。

### 场景 E：Agent 搭建 / 自动化
- **翻译侧重**：翻译任务描述，但保留工具名、参数键、API 端点。
- **示例**：用户"每天早上8点用 sendEmail 工具给团队发昨日报告" → 锚点"every day at 8am use the sendEmail tool to send yesterday's report to the team"。
- **附加**：翻译后的英语锚点可直接作为 automation prompt 的内部表示。

## 与 `<response_language>` 的关系

这是最容易混淆的一点，单独说明：

| 维度 | Enfirst Bridge | `<response_language>` |
|---|---|---|
| 作用层 | 输入理解层（模型怎么"读"输入） | 输出表达层（模型用什么语言"说"） |
| 方向 | 非英语 → 英语（理解锚点） | 英语语义 → 用户语言（输出） |
| 是否可见 | 对用户透明（内部步骤） | 对用户可见（最终回复语言） |

**正确链路**：用户中文输入 → Enfirst 翻译成英语锚点 → 模型用英语思考 → 按 `<response_language>` 用中文输出。

## Token 节省估算

- 短输入（<50 字）：节省约 5-15% 的理解 token（内部对齐开销占比小）。
- 中等输入（50-500 字）：节省约 15-25%（内部对齐开销显著）。
- 长上下文（>500 字 / 多轮）：节省可达 25-35%，且语义漂移修正带来的稳定性提升比 token 节省更有价值。

> 注：实际节省取决于模型版本与输入语种。CJK 语种（中日韩）节省最明显，因为字符密度高、内部对齐开销大。

## 文件结构

```
enfirst-bridge/
├── SKILL.md                  ← 本文件（主指令）
├── README.md                 ← GitHub 展示用说明
├── scripts/
│   ├── detect_lang.py        ← 语种检测脚本
│   ├── translate.py          ← 直线翻译核心脚本（可被 Agent 调用）
│   └── test_bridge.py        ← 5 场景测试用例
├── references/
│   ├── scenario_rules.md     ← 5 场景翻译规则详解
│   └── token_benchmark.md    ← token 节省基准测试方法
└── examples/
    ├── dialog_example.md     ← 对话场景示例
    ├── code_example.md       ← 编程场景示例
    ├── image_example.md      ← 图片生成场景示例
    ├── copy_example.md       ← 文案场景示例
    └── agent_example.md      ← Agent 场景示例
```

## 安装

### WorkBuddy / CodeBuddy
将本目录复制到：
- 项目级：`<workspace>/.workbuddy/skills/enfirst-bridge/`
- 用户级：`~/.workbuddy/skills/enfirst-bridge/`

### Cursor
复制到 `<project>/.cursor/skills/enfirst-bridge/`

### 通用
任何支持 SKILL.md 约定的 AI 编程助手均可使用本目录。

## 使用方式

### 自动模式（默认）
加载本 skill 后，模型在每次用户输入非英语时**自动**执行 Enfirst Bridge 流程，用户无感知。

### 显式模式
用户可说"用英语理解"或"先翻译再处理"来强制触发。

### 脚本模式（供 Agent / 自动化调用）
```bash
python scripts/translate.py --input "用户输入文本" --scenario auto
# 输出：英语锚点文本
```

## 限制与边界

- **不适用于**：用户明确要求"用我的母语思考"的场景；纯情感陪伴对话（翻译会破坏语气温度，此时应关闭本 skill）。
- **翻译质量**：本 skill 的翻译是"理解用直译"，不是"交付用译文"。若用户要的是专业翻译交付，应使用专门的翻译 skill，而非本 skill。
- **多语言混合输入**：一段话里中英混排时，只翻译非英语部分，英语部分原样保留。

## 许可证

MIT — 自由使用、修改、分发。
